# Microsoft Teams (incoming webhook)

- **connector_id:** `microsoft_teams`
- **Config:** `webhook_url`
- **Inbound:** dict with `title`, `message`. Adaptive/MessageCard-style payload sent when `THMP_CONNECTOR_LIVE_HTTP=1`.
