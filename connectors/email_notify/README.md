# Email (SMTP) notifications

- **connector_id:** `email_smtp`
- **Config:** `smtp_host`, `smtp_port`, `smtp_user`, `smtp_password`, `from_address`, `to_addresses` (comma-separated or list).
- **Live sends** only when `THMP_CONNECTOR_LIVE_HTTP=1`.
