# Slack notifications

- **connector_id:** `slack`
- **Config:** `webhook_url`, optional `trigger_on` (list of event types).
- **Inbound:** Event dict with `event_type`, `message`. Does not create hypotheses.
