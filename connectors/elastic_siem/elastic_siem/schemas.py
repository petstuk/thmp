from __future__ import annotations

# kibana.alert.severity → THMP severity
ELASTIC_SEVERITY_MAP: dict[str, str] = {
    "critical": "critical",
    "high": "high",
    "medium": "medium",
    "low": "low",
}
