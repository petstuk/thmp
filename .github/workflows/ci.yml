name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend/hypothesis_service
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: |
          pip install -e ../thmp_common -e .
          pip install -e ../attack_service
          pip install -e ../user_service
          pip install -e ../audit_service
          pip install -e ../ingestion_service
          pip install -e ../thmp_cdk
          pip install -e ../../connectors/example_webhook
          pip install pytest ruff httpx
      - name: Ruff
        run: |
          ruff check ../thmp_common ../thmp_cdk ../user_service/app ../user_service/tests \
            ../audit_service/app ../audit_service/tests \
            ../hypothesis_service/app ../hypothesis_service/tests ../attack_service/app ../attack_service/tests \
            ../ingestion_service/app ../ingestion_service/tests ../../connectors/example_webhook
      - name: Pytest
        run: |
          pytest -q
          cd ../attack_service && pytest -q
          cd ../thmp_cdk && pytest -q
          cd ../../connectors/example_webhook && pytest -q
          cd ../../backend/user_service && pytest -q
          cd ../../backend/audit_service && pytest -q
          cd ../../backend/ingestion_service && pytest -q

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npx tsc -b --noEmit
      - run: npm run build

  smoke-compose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Env file
        run: cp .env.example .env
      - name: Start stack
        run: docker compose up -d --build
      - name: Wait for service health
        run: |
          set -e
          for i in $(seq 1 90); do
            if curl -fsS http://127.0.0.1:8001/health/live >/dev/null 2>&1 \
              && curl -fsS http://127.0.0.1:8002/health/live >/dev/null 2>&1 \
              && curl -fsS http://127.0.0.1:8003/health/live >/dev/null 2>&1 \
              && curl -fsS http://127.0.0.1:8004/health/live >/dev/null 2>&1 \
              && curl -fsS http://127.0.0.1:8005/health/live >/dev/null 2>&1; then
              break
            fi
            sleep 2
          done
          curl -fsS http://127.0.0.1:8001/health/live
          curl -fsS http://127.0.0.1:8002/health/live
          curl -fsS http://127.0.0.1:8003/health/live
          curl -fsS http://127.0.0.1:8004/health/live
          curl -fsS http://127.0.0.1:8005/health/live
          sleep 5
      - name: Smoke API (Traefik)
        run: SMOKE_BASE_URL=http://127.0.0.1 python3 scripts/smoke_api.py
